import axios from "axios";

const state = {
  trays: [],
};

const getters = {
  allTrays: (state) => state.trays,
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
